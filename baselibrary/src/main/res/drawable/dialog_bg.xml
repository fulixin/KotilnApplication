<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 圆角 -->
    <corners android:radius="@dimen/dialog_bg_radius" /><!-- 设置圆角半径 -->

    <!-- 间隔 -->
    <padding
        android:bottom="@dimen/dialog_bg_padding"
        android:left="@dimen/dialog_bg_padding"
        android:right="@dimen/dialog_bg_padding"
        android:top="@dimen/dialog_bg_padding" /><!-- 各方向的间隔 -->

    <!-- 大小 -->
    <size
        android:width="@dimen/dialog_bg_width"
        android:height="@dimen/dialog_bg_height" /><!-- 宽度和高度 -->

    <!-- 填充 -->
    <solid android:color="@color/White" /><!-- 填充的颜色 -->

    <!-- 描边 -->
    <stroke
        android:width="@dimen/dialog_bg_stroke_width"
        android:color="@color/Gainsboro"/>
</shape>